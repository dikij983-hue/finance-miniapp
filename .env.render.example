# ═══════════════════════════════════════════════════════════════════
# НЕ ВСТАВЛЯЙТЕ СЮДА РЕАЛЬНЫЙ КЛЮЧ — этот файл в Git, все его увидят.
# Ключ только в файле:  .env.render  (рядом, БЕЗ суффикса .example)
# ═══════════════════════════════════════════════════════════════════
#
# Один раз скопируйте шаблон в локальный секретный файл:
#   cp .env.render.example .env.render
# Потом откройте .env.render и после = вставьте ключ Render.
#
RENDER_API_KEY=
